name: Services Migrate CSV
type: module
description: Migration of the different services links that OSU has into drupal
package: Migration
version: 1.1.0
core_version_requirement: ^9 || ^10
dependencies:
  - drupal:migrate
  - migrate_plus:migrate_plus
  - migrate_source_csv:migrate_source_csv (>=8.x-3.0)
